function closeContent() {
                var a = document.getElementById('content');

                a.style.padding = "0";
                a.style.height = "0";

function login (){
            var question = "enter username";
            var response = prompt(question);
             var question = "enter password";
             var response = prompt(question);
            }
            
            function signUp(){
            var question = "Enter Username";
             var response = prompt(question);
            var question = "Enter password";
            var response = prompt(question);
            }
